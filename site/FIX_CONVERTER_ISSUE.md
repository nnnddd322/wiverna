# 🔧 Исправление проблемы конвертации

## ✅ Хорошие новости
Система работает! Вы успешно:
- ✅ Загрузили PPTX файл
- ✅ Frontend показывает экран обработки
- ✅ Polling работает (попытка 17 из 60)
- ✅ База данных настроена
- ✅ Converter запущен

## ❌ Проблема
**LibreOffice не установлен в Windows**

Ошибка в логах converter:
```
"libreoffice" не является внутренней или внешней
командой, исполняемой программой или пакетным файлом.
```

Converter не может конвертировать PPTX без LibreOffice.

---

## 🚀 Решение: Использовать Docker

У вас Docker установлен (видел на скриншоте VS Code). Используйте Docker Compose - там LibreOffice уже в контейнере.

### Шаг 1: Остановить локальные процессы

В терминалах где запущены сервисы нажмите **Ctrl+C**:
- Терминал с `npm run dev` (frontend)
- Терминал с `npm start` (converter)

### Шаг 2: Запустить через Docker

```powershell
cd c:\Users\nnndd\OneDrive\Desktop\v2.0

# Запустить оба сервиса
docker compose up --build
```

**Важно:** Используйте `docker compose` (с пробелом), не `docker-compose`.

### Шаг 3: Дождаться запуска

Вы увидите логи обоих сервисов:
```
frontend_1  | VITE ready in 1000ms
converter_1 | Converter service running on port 8787
```

### Шаг 4: Открыть приложение

**Frontend:** http://localhost:5173  
**Converter:** http://localhost:8787

### Шаг 5: Переконвертировать презентацию

Презентация сейчас в статусе "processing" но с ошибкой. Нужно сбросить статус:

**Вариант A: Через UI (проще)**
1. Откройте презентацию
2. Нажмите кнопку загрузки (иконка Upload в топ-баре)
3. Загрузите тот же или новый PPTX файл
4. Дождитесь конвертации

**Вариант B: Через SQL**
```sql
-- В Supabase SQL Editor
UPDATE presentations 
SET status = 'processing', 
    error_message = NULL,
    slides_data = NULL
WHERE lecture_id = 'e74acd41-e8ad-43b6-96e1-7d169eae70fd';
```

Затем вызовите converter API:
```powershell
curl -X POST http://localhost:8787/convert `
  -H "Content-Type: application/json" `
  -d '{"lectureId": "e74acd41-e8ad-43b6-96e1-7d169eae70fd"}'
```

---

## 🐛 Если Docker не запускается

### Проверка 1: Docker Desktop запущен?
Откройте Docker Desktop из меню Пуск.

### Проверка 2: Docker в PATH?
```powershell
# Найти где установлен Docker
Get-Command docker

# Если не найден, добавить в PATH:
# C:\Program Files\Docker\Docker\resources\bin
```

### Проверка 3: WSL2 настроен?
Docker Desktop для Windows требует WSL2.

Если проблемы с Docker - используйте альтернативное решение ниже.

---

## 🔄 Альтернатива: Установить LibreOffice локально

Если Docker не работает, установите LibreOffice:

### Шаг 1: Скачать
https://www.libreoffice.org/download/download/

Выберите Windows 64-bit.

### Шаг 2: Установить
Запустите установщик, следуйте инструкциям.

### Шаг 3: Добавить в PATH

**PowerShell (от администратора):**
```powershell
$env:Path += ";C:\Program Files\LibreOffice\program"
[Environment]::SetEnvironmentVariable("Path", $env:Path, [System.EnvironmentVariableTarget]::Machine)
```

Или вручную:
1. Win + R → `sysdm.cpl` → Enter
2. "Дополнительно" → "Переменные среды"
3. В "Системные переменные" найдите `Path`
4. "Изменить" → "Создать" → `C:\Program Files\LibreOffice\program`
5. OK → OK → OK

### Шаг 4: Проверить
```powershell
# Перезапустить PowerShell
libreoffice --version
```

Должно показать версию.

### Шаг 5: Перезапустить converter
```powershell
cd c:\Users\nnndd\OneDrive\Desktop\v2.0\converter
npm start
```

### Шаг 6: Переконвертировать
Перезагрузите PPTX через UI или используйте SQL + API выше.

---

## ✅ Проверка успешной конвертации

После запуска Docker или установки LibreOffice:

1. **Проверить converter health:**
```powershell
curl http://localhost:8787/health
```

2. **Посмотреть логи converter:**
Должны увидеть:
```
Processing conversion for lecture e74acd41-...
Converting PPTX to PDF...
Converting PDF to PNG...
Uploading slides...
Conversion completed successfully
```

3. **В браузере:**
Страница автоматически обновится и покажет слайды.

4. **В БД:**
```sql
SELECT status, slides_data->>'pageCount' as slides 
FROM presentations 
WHERE lecture_id = 'e74acd41-e8ad-43b6-96e1-7d169eae70fd';
```

Должно быть `status = 'ready'` и количество слайдов.

---

## 🎯 Рекомендация

**Используйте Docker** - это проще и надежнее:
- LibreOffice уже установлен
- Одинаковое окружение везде
- Не нужно настраивать PATH

Команда:
```powershell
docker compose up --build
```

После этого презентации будут конвертироваться автоматически! 🚀
