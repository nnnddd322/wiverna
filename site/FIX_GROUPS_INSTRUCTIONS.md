# 🔧 Инструкция по исправлению проблем с группами

## Проблемы
1. ❌ Невозможно создать группу: `new row violates row-level security policy for table "groups"`
2. ❌ Невозможно удалить группу: `Cannot coerce the result to a single JSON object`
3. ❌ Группы не отображаются в списке при регистрации
4. ❌ В панели преподавателя отображается "Неизвестно" вместо реальных студентов

## Решение

### Шаг 1: Применить миграцию RLS для таблицы groups

Выполните SQL миграцию в Supabase:

```sql
-- Fix RLS policies for groups table
-- Drop existing policies if any
DROP POLICY IF EXISTS "groups_select" ON groups;
DROP POLICY IF EXISTS "groups_insert" ON groups;
DROP POLICY IF EXISTS "groups_update" ON groups;
DROP POLICY IF EXISTS "groups_delete" ON groups;

-- Enable RLS
ALTER TABLE groups ENABLE ROW LEVEL SECURITY;

-- Allow everyone to read groups (for registration)
CREATE POLICY "groups_select" ON groups
  FOR SELECT
  USING (true);

-- Only teachers can insert groups
CREATE POLICY "groups_insert" ON groups
  FOR INSERT
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM profiles
      WHERE profiles.id = auth.uid()
      AND profiles.role = 'teacher'
    )
  );

-- Only teachers can update groups
CREATE POLICY "groups_update" ON groups
  FOR UPDATE
  USING (
    EXISTS (
      SELECT 1 FROM profiles
      WHERE profiles.id = auth.uid()
      AND profiles.role = 'teacher'
    )
  );

-- Only teachers can delete groups
CREATE POLICY "groups_delete" ON groups
  FOR DELETE
  USING (
    EXISTS (
      SELECT 1 FROM profiles
      WHERE profiles.id = auth.uid()
      AND profiles.role = 'teacher'
    )
  );
```

### Шаг 2: Как применить миграцию

#### Вариант А: Через Supabase Dashboard
1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте и вставьте SQL из файла `supabase/migrations/20260113_fix_groups_rls.sql`
5. Нажмите **Run**

#### Вариант Б: Через Supabase CLI
```bash
# Если у вас установлен Supabase CLI
supabase db push
```

### Шаг 3: Проверка

После применения миграции:

1. **Войдите как преподаватель**
2. **Откройте панель преподавателя**
3. **Нажмите "Управление группами"**
4. **Создайте тестовую группу** (например, "ТМ-22")
5. **Проверьте, что группа создалась**

### Шаг 4: Проверка регистрации

1. **Выйдите из системы**
2. **Перейдите на страницу регистрации**
3. **Откройте консоль браузера** (F12)
4. **Проверьте логи:**
   - Должно быть: `Loaded groups: [{id: "...", name: "ТМ-22"}]`
   - Если нет групп: `No groups found in database`
5. **Раскройте список групп** - должна отображаться созданная группа

---

## Что было исправлено

### 1. RLS политики для таблицы `groups`

**Проблема:** Не было правильных RLS политик

**Решение:**
- ✅ `SELECT` - доступен всем (для регистрации)
- ✅ `INSERT` - только преподавателям
- ✅ `UPDATE` - только преподавателям
- ✅ `DELETE` - только преподавателям

### 2. Логика отображения студентов в TeacherDashboard

**Проблема:** Отображались записи `discipline_access` с `group_id`, но без реальных студентов

**Решение:**
```typescript
// Фильтруем только записи с реальными студентами
.not('student_id', 'is', null)

// Дополнительная фильтрация на фронтенде
const validAccess = accessData?.filter((a: any) => 
  a.profiles && a.profiles.id
) || [];
```

### 3. Отладочная информация в RegisterPage

**Добавлено:**
- Логирование загруженных групп в консоль
- Предупреждение, если групп нет
- Сообщение об ошибке при проблемах с загрузкой

---

## Тестирование

### Тест 1: Создание группы
```
✅ Войти как преподаватель
✅ Панель преподавателя → Управление группами
✅ Ввести название "ТМ-22"
✅ Нажать "+"
✅ Группа должна появиться в списке
```

### Тест 2: Редактирование группы
```
✅ Нажать кнопку редактирования (карандаш)
✅ Изменить название на "ТМ-23"
✅ Нажать "Сохранить"
✅ Название должно обновиться
```

### Тест 3: Удаление группы
```
✅ Нажать кнопку удаления (корзина)
✅ Подтвердить удаление
✅ Группа должна исчезнуть из списка
```

### Тест 4: Регистрация студента
```
✅ Выйти из системы
✅ Страница регистрации
✅ Открыть консоль (F12)
✅ Раскрыть список групп
✅ Должны отображаться созданные группы
✅ Выбрать группу
✅ Заполнить данные
✅ Зарегистрироваться
```

### Тест 5: Отображение студентов
```
✅ Войти как преподаватель
✅ Создать дисциплину
✅ Выдать доступ группе через "Дисциплины"
✅ Зарегистрировать студента в этой группе
✅ Панель преподавателя → выбрать дисциплину
✅ Должен отображаться студент с его данными
```

---

## Проверка RLS политик

Выполните в SQL Editor:

```sql
-- Проверка существующих политик для groups
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual,
  with_check
FROM pg_policies
WHERE tablename = 'groups';
```

**Ожидаемый результат:** 4 политики
- `groups_select` (SELECT)
- `groups_insert` (INSERT)
- `groups_update` (UPDATE)
- `groups_delete` (DELETE)

---

## Частые ошибки

### Ошибка: "new row violates row-level security policy"
**Причина:** Нет политики INSERT или пользователь не преподаватель  
**Решение:** Применить миграцию и войти как преподаватель

### Ошибка: Группы не загружаются
**Причина:** Нет политики SELECT  
**Решение:** Применить миграцию

### Ошибка: "Неизвестно" в панели преподавателя
**Причина:** Нет реальных студентов, только доступ для группы  
**Решение:** Зарегистрировать студентов в группе

---

## Дополнительная информация

### Структура таблицы groups
```sql
CREATE TABLE groups (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Связь с profiles
```sql
-- В таблице profiles
group_id UUID REFERENCES groups(id)
```

### Связь с discipline_access
```sql
-- В таблице discipline_access
group_id UUID REFERENCES groups(id)  -- Для группового доступа
student_id UUID REFERENCES profiles(id)  -- Для индивидуального доступа
```

---

## Поддержка

Если проблемы остались:

1. Проверьте консоль браузера (F12) на ошибки
2. Проверьте логи Supabase
3. Убедитесь, что миграция применена
4. Проверьте, что вы вошли как преподаватель

**Файлы:**
- Миграция: `supabase/migrations/20260113_fix_groups_rls.sql`
- Компонент: `src/app/components/GroupManagementDialog.tsx`
- Сервис: `src/services/groupService.ts`
