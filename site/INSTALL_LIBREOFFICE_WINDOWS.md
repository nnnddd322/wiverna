# 📦 Установка LibreOffice для Windows

## 🎯 Цель
Установить LibreOffice чтобы converter мог конвертировать PPTX файлы.

---

## 🚀 Быстрая установка

### Шаг 1: Скачать LibreOffice
1. Откройте браузер
2. Перейдите: https://www.libreoffice.org/download/download/
3. Нажмите "Download" для Windows 64-bit
4. Сохраните файл

### Шаг 2: Установить
1. Запустите загруженный файл (обычно `LibreOffice_..._Win_x64.msi`)
2. Нажмите "Run"
3. Выберите "Typical" (обычная установка)
4. Нажмите "Install"
5. Дождитесь завершения
6. Нажмите "Finish"

### Шаг 3: Добавить в PATH (ВАЖНО!)

**Способ A: PowerShell (проще)**
```powershell
# Откройте PowerShell от имени администратора
# Win + X → "Windows PowerShell (администратор)"

$env:Path += ";C:\Program Files\LibreOffice\program"
[Environment]::SetEnvironmentVariable("Path", $env:Path, [System.EnvironmentVariableTarget]::Machine)

# Перезапустите PowerShell
```

**Способ B: Через интерфейс Windows**
1. Win + R → `sysdm.cpl` → Enter
2. "Дополнительно" → "Переменные среды"
3. В "Системные переменных" найдите `Path`
4. "Изменить" → "Создать"
5. Введите: `C:\Program Files\LibreOffice\program`
6. OK → OK → OK

### Шаг 4: Проверить установку
```powershell
# Перезапустите PowerShell
libreoffice --version
```

Должно показать что-то вроде:
```
LibreOffice 7.6.4 40c07164f3e41eea77e8a5a00d4b07c0d9b22420
```

### Шаг 5: Перезапустить converter
```powershell
cd c:\Users\nnndd\OneDrive\Desktop\v2.0\converter
npm start
```

---

## 🔄 После установки

### 1. Проверить converter
```powershell
curl http://localhost:8787/health
```

### 2. Переконвертировать презентацию
**Вариант A: Через UI (рекомендуется)**
1. Откройте http://localhost:5173
2. Войдите как teacher
3. Откройте презентацию
4. Нажмите кнопку Upload (иконка загрузки)
5. Загрузите тот же PPTX файл
6. Дождитесь конвертации (10-60 секунд)

**Вариант B: Через API**
```powershell
curl -X POST http://localhost:8787/convert `
  -H "Content-Type: application/json" `
  -d '{"lectureId": "e74acd41-e8ad-43b6-96e1-7d169eae70fd"}'
```

---

## ✅ Успешная конвертация

После установки LibreOffice вы увидите в логах converter:
```
Processing conversion for lecture e74acd41-...
Converting PPTX to PDF...
Converting PDF to PNG...
Uploading slides...
Conversion completed successfully
```

И в браузере через 10-60 секунд появятся слайды!

---

## 🐛 Если что-то не работает

### Проблема: "libreoffice не найден"
**Решение:** Перезапустите PowerShell и проверьте PATH:
```powershell
echo $env:Path
# Должно содержать: C:\Program Files\LibreOffice\program
```

### Проблема: LibreOffice установился в другую папку
**Решение:** Найдите где установлен:
```powershell
Get-ChildItem -Path "C:\Program Files" -Recurse -Filter "soffice.exe"
```

И добавьте правильный путь в PATH.

### Проблема: Ошибка конвертации
**Решение:** Проверьте что PPTX файл не поврежден и не защищен паролем.

---

## 🎉 Готово!

После установки LibreOffice презентации будут конвертироваться автоматически! 🚀

**Следующий шаг:** Установите LibreOffice и перезагрузите PPTX файл через UI.
