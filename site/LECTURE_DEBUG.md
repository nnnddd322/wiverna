# Диагностика проблемы с лекциями

## 🐛 Проблема

При входе в дисциплину:
1. Вся страница становится кликабельной (курсор-указатель)
2. При клике на пустое место открывается пустая презентация
3. Нет отображения лекций/материалов

## 🔍 Что нужно проверить

### 1. Консольные логи

**Сделайте Ctrl+F5 и зайдите в дисциплину. В консоли должно быть:**

```
=== loadLectures called === {disciplineId: "...", isTeacher: true/false}
=== loadLectures result === {count: X, data: [...]}
=== Lecture filtering results === {
  total: X,
  presentations: X, 
  textLectures: X,
  tests: X,
  lectures: [...]
}
```

**Если лекций нет (count: 0):**
- Проверьте, есть ли лекции в Supabase
- Проверьте `discipline_id` у лекций

**Если лекции есть, но не фильтруются:**
- Проверьте поле `type` у лекций
- Должны быть: 'presentation', 'article', 'test'

### 2. Проверка в Supabase

```sql
-- Проверить лекции для вашей дисциплины
SELECT id, title, type, discipline_id, status, created_at
FROM lectures 
WHERE discipline_id = 'ВАШ_DISCIPLINE_ID'
ORDER BY created_at DESC;

-- Проверить типы лекций
SELECT type, COUNT(*) as count
FROM lectures 
WHERE discipline_id = 'ВАШ_DISCIPLINE_ID'
GROUP BY type;
```

### 3. Возможные причины проблемы

#### A. Лекции не загружаются
- **Причина**: RLS политики блокируют доступ
- **Решение**: Проверить, что `FIX_DROP_ALL_POLICIES_AND_SIMPLE_RLS.sql` выполнен

#### B. Лекции загружаются, но не фильтруются
- **Причина**: Неправильные значения в поле `type`
- **Решение**: Обновить типы лекций в Supabase

#### C. Проблема с рендерингом
- **Причина**: Пустой массив лекций, но Tabs всё равно рендерится
- **Решение**: Добавлена проверка `lectures.length > 0` перед рендерингом Tabs

## 🛠️ Быстрые исправления

### Если лекций нет в Supabase:

```sql
-- Создать тестовую лекцию
INSERT INTO lectures (
  id, 
  discipline_id, 
  title, 
  type, 
  content, 
  status, 
  order_index, 
  created_at, 
  updated_at
) VALUES (
  gen_random_uuid(), 
  'ВАШ_DISCIPLINE_ID', 
  'Тестовая лекция', 
  'article', 
  'Это содержимое тестовой лекции',
  'published',
  0,
  NOW(),
  NOW()
);
```

### Если типы лекций неправильные:

```sql
-- Исправить типы лекций
UPDATE lectures 
SET type = 'article' 
WHERE type NOT IN ('presentation', 'article', 'test');

-- Или установить конкретные типы
UPDATE lectures 
SET type = 'presentation' 
WHERE title LIKE '%презентация%' OR title LIKE '%слайд%';

UPDATE lectures 
SET type = 'test' 
WHERE title LIKE '%тест%' OR title LIKE '%опрос%';
```

## 🎯 Что должно работать после исправлений

1. **Если лекций нет**: Показать сообщение "В этой дисциплине пока нет материалов"
2. **Если лекции есть**: Показать вкладки с фильтрацией по типам
3. **Кликабельность**: Только карточки лекций должны быть кликабельными

## 📝 Следующие шаги

1. **Ctrl+F5** и зайдите в дисциплину
2. **Скопируйте все логи** из консоли (особенно `=== loadLectures result ===`)
3. **Проверьте в Supabase** наличие лекций для вашей дисциплины
4. **Отправьте логи** для дальнейшей диагностики

## ⚠️ Важно

Проблема с "кликабельной всей страницей" обычно возникает когда:
- Компонент рендерится без контента
- Есть невидимый элемент с `onClick`
- CSS стили делают всю область кликабельной

Добавленные проверки должны решить эту проблему.
