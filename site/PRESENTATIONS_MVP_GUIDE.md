# MVP Презентаций - Руководство по запуску

## 📋 Что реализовано

### ✅ P0 - Блокеры (Готово)

**PRS-0.1: Storage bucket и структура хранения**
- ✅ SQL миграция создает bucket `presentations`
- ✅ Стандартные пути:
  - `presentations/<lectureId>/source.pptx` - исходный файл
  - `presentations/<lectureId>/slides/001.png, 002.png...` - слайды
- ✅ RLS политики настроены

**PRS-0.2: Формат slides_data и статусы**
- ✅ JSON формат зафиксирован в коде
- ✅ Статусы: `pending`, `processing`, `ready`, `error`
- ✅ Поле `error_message` для ошибок

**PRS-0.3: 1 lecture → 1 presentations запись**
- ✅ UNIQUE constraint на `lecture_id`
- ✅ Upsert логика в `presentationService`
- ✅ Повторная загрузка перезаписывает файл и удаляет старые слайды

### ✅ P1 - Frontend (Готово)

**PRS-1.1-1.3: UI загрузки и конвертации**
- ✅ Кнопка "Загрузить PPTX" для преподавателей
- ✅ File picker только .pptx
- ✅ UI состояния: idle → uploading → processing → ready/error
- ✅ Upload в Storage по правильному пути
- ✅ Upsert в таблицу `presentations`
- ✅ Автоматический вызов converter API
- ✅ Обработка ошибок с понятными сообщениями

**PRS-3.1-3.2: Viewer с polling**
- ✅ Polling статуса каждые 3 сек (макс 60 попыток = 3 мин)
- ✅ Состояния: loading, no file, processing, error, ready
- ✅ Отображение PNG слайдов из Storage
- ✅ Навигация: стрелки, клавиатура (←, →, Space, Enter)
- ✅ Зум +/- (50% - 200%)
- ✅ Fullscreen режим
- ✅ Счетчик слайдов "3 / 20"
- ✅ Кнопка повторной загрузки для преподавателей

### ✅ P1 - Backend: Converter Service (Готово)

**PRS-2.1-2.3: Converter сервис**
- ✅ Dockerfile с LibreOffice + poppler-utils
- ✅ Node.js Express API
- ✅ Endpoint POST /convert
- ✅ Пайплайн: PPTX → PDF (LibreOffice) → PNG (pdftoppm)
- ✅ Upload слайдов в Storage
- ✅ Обновление `slides_data` в БД
- ✅ Обработка ошибок с записью в `error_message`
- ✅ Логирование всех этапов
- ✅ Очистка временных файлов

**PRS-5.1: Docker Compose**
- ✅ `docker-compose.yml` для frontend + converter
- ✅ Переменные окружения настроены
- ✅ Сетевое взаимодействие между сервисами

---

## 🚀 Инструкция по запуску

### Шаг 1: Запустить SQL миграцию

```bash
# Выполните миграцию в Supabase SQL Editor
cat supabase/migrations/20260108_presentations_setup.sql
```

Миграция создаст:
- Таблицу `presentations`
- Storage bucket `presentations`
- RLS политики
- Индексы

### Шаг 2: Настроить переменные окружения

**Для frontend (`.env`):**
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_CONVERTER_URL=http://localhost:8787
```

**Для converter (`converter/.env`):**
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
PORT=8787
PRESENTATIONS_BUCKET=presentations
```

⚠️ **Важно:** Converter использует `service_role` ключ, не `anon` ключ!

### Шаг 3: Запустить через Docker Compose

```bash
# Из корня проекта
docker-compose up

# Или в фоновом режиме
docker-compose up -d
```

Сервисы будут доступны:
- Frontend: http://localhost:5173
- Converter: http://localhost:8787

### Шаг 4: Проверить работу converter

```bash
curl http://localhost:8787/health
```

Ответ:
```json
{
  "status": "ok",
  "service": "presentation-converter",
  "timestamp": "2026-01-08T19:00:00.000Z"
}
```

---

## 📝 Acceptance критерии (как проверить)

### ✅ Преподаватель:

1. **Создать презентацию:**
   - Войти как преподаватель
   - Создать дисциплину (если нет)
   - Добавить материал типа "Презентация"
   - Открыть созданную презентацию

2. **Загрузить PPTX:**
   - Нажать "Выбрать файл PPTX"
   - Выбрать .pptx файл
   - Увидеть "Загрузка..." → "Обрабатываем презентацию"
   - Подождать 10-60 секунд (зависит от размера)

3. **Увидеть готовые слайды:**
   - Страница автоматически обновится
   - Появится просмотрщик с PNG слайдами
   - Навигация стрелками работает
   - Зум +/- работает

4. **Перезалить PPTX:**
   - Нажать кнопку Upload в топ-баре
   - Выбрать новый файл
   - Слайды обновятся (старые удалятся)

### ✅ Студент:

1. **Открыть дисциплину:**
   - Войти как студент
   - Открыть дисциплину с доступом
   - Увидеть презентацию в списке материалов

2. **Просмотреть слайды:**
   - Кликнуть на презентацию
   - Если обработка идёт → увидеть лоадер "Обрабатываем..."
   - Если готово → увидеть слайды
   - Листать стрелками / клавиатурой
   - Использовать зум и fullscreen

3. **Если ошибка:**
   - Увидеть сообщение об ошибке
   - Понять, что нужно делать (обратиться к преподавателю)

---

## 🔧 Troubleshooting

### Converter не запускается

**Проблема:** `Error: Cannot find module 'express'`
```bash
cd converter
npm install
```

**Проблема:** LibreOffice не найден
```bash
# В Docker контейнере уже установлен
# Для локальной разработки:
# macOS: brew install libreoffice
# Ubuntu: apt-get install libreoffice
# Windows: скачать с libreoffice.org
```

### Презентация застряла в "processing"

**Причины:**
1. Converter не запущен → проверить `docker-compose ps`
2. Converter не может подключиться к Supabase → проверить `SUPABASE_URL` и ключ
3. Ошибка конвертации → проверить логи `docker-compose logs converter`

**Решение:**
```bash
# Проверить логи
docker-compose logs converter

# Перезапустить converter
docker-compose restart converter

# Повторно загрузить PPTX
```

### Слайды не отображаются

**Причины:**
1. Bucket не публичный
2. RLS политики блокируют доступ
3. Неправильные пути к файлам

**Решение:**
```sql
-- Проверить bucket
SELECT * FROM storage.buckets WHERE id = 'presentations';

-- Проверить файлы
SELECT * FROM storage.objects WHERE bucket_id = 'presentations';

-- Проверить RLS политики
SELECT * FROM pg_policies WHERE tablename = 'objects';
```

### Frontend не видит converter

**Проблема:** `Сервис конвертации недоступен`

**Решение:**
1. Проверить `VITE_CONVERTER_URL` в `.env`
2. Для локальной разработки: `http://localhost:8787`
3. Для Docker: `http://converter:8787` (внутри сети)
4. Проверить CORS в converter (уже настроен)

---

## 📊 Структура данных

### Таблица `presentations`

```sql
CREATE TABLE presentations (
  id UUID PRIMARY KEY,
  lecture_id UUID UNIQUE NOT NULL,  -- FK to lectures
  file_path TEXT,                    -- "lectureId/source.pptx"
  status TEXT NOT NULL,              -- pending | processing | ready | error
  error_message TEXT,
  slides_data JSONB,                 -- см. ниже
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```

### Формат `slides_data`

```json
{
  "pageCount": 20,
  "slides": [
    {
      "index": 1,
      "path": "lecture-uuid/slides/001.png",
      "width": 1920,
      "height": 1080
    },
    {
      "index": 2,
      "path": "lecture-uuid/slides/002.png",
      "width": 1920,
      "height": 1080
    }
  ]
}
```

### Storage структура

```
presentations/
├── <lecture-uuid-1>/
│   ├── source.pptx
│   └── slides/
│       ├── 001.png
│       ├── 002.png
│       └── ...
├── <lecture-uuid-2>/
│   ├── source.pptx
│   └── slides/
│       └── ...
```

---

## 🎯 Следующие шаги (опционально)

### P2 - Улучшения

1. **Thumbnail генерация:**
   - Создавать `thumb.png` из первого слайда
   - Показывать в списке материалов

2. **Прогресс загрузки:**
   - Progress bar при upload файла
   - Процент конвертации (сложно, требует WebSocket)

3. **Оптимизация:**
   - Ленивая загрузка слайдов (preload ±1)
   - Кеширование в браузере
   - WebP вместо PNG (меньше размер)

4. **Интеграция в TeacherDashboard:**
   - Кнопка "Добавить презентацию" в UI
   - Список презентаций с статусами

---

## 📚 Документация

- **Frontend:** `src/app/components/PresentationViewer.tsx`
- **Service:** `src/services/presentationService.ts`
- **Converter:** `converter/README.md`
- **Migration:** `supabase/migrations/20260108_presentations_setup.sql`
- **Docker:** `docker-compose.yml`

---

## ✨ Готово!

MVP презентаций полностью реализован и готов к использованию. Все acceptance критерии выполнены:

✅ Преподаватель может загрузить PPTX  
✅ Конвертация происходит автоматически  
✅ Студент видит готовые слайды  
✅ Повторная загрузка обновляет презентацию  
✅ Обработка ошибок с понятными сообщениями  
✅ Polling автоматически обновляет статус  
✅ UI качественный и удобный  

**Запуск:** `docker-compose up` и всё работает! 🚀
