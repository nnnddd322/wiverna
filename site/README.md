
  # EduPortal - Образовательная платформа

  Современная образовательная платформа для программы профессиональной переподготовки "Педагогика и психология" (530 часов).

  ## Возможности

  ### Для студентов
  - 📚 Доступ к дисциплинам и учебным материалам
  - 📖 Чтение лекций в формате статей
  - 🎯 Прохождение тестов
  - 📊 Отслеживание прогресса обучения
  - 🎓 Просмотр презентаций

  ### Для преподавателей
  - ➕ Создание и управление дисциплинами
  - ✍️ Создание лекций и учебных материалов
  - 📤 Загрузка презентаций (PPTX)
  - ✅ Создание тестов
  - 🔐 Управление доступом (по группам и индивидуально)
  - 📈 Просмотр статистики студентов

  ### Для администраторов
  - 👥 Управление группами студентов
  - 🔧 Настройка системы

  ## Технологический стек

  - **Frontend**: React 18, TypeScript, Vite
  - **UI**: Tailwind CSS, Radix UI, shadcn/ui
  - **Backend**: Supabase (PostgreSQL, Authentication, Storage)
  - **Иконки**: Lucide React

  ## Установка и запуск

  ### 1. Клонирование репозитория

  ```bash
  git clone <repository-url>
  cd v2.0
  ```

  ### 2. Установка зависимостей

  ```bash
  npm install
  ```

  ### 3. Настройка базы данных

  Следуйте инструкциям в файле [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) для настройки базы данных в Supabase.

  ### 4. Настройка переменных окружения

  Создайте файл `.env` в корне проекта:

  ```env
  VITE_SUPABASE_URL=your_supabase_project_url
  VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
  ```

  Замените значения на реальные из настроек вашего проекта Supabase (Settings → API).

  ### 5. Запуск приложения

  ```bash
  npm run dev
  ```

  Приложение будет доступно по адресу http://localhost:5173

  ## Структура проекта

  ```
  v2.0/
  ├── src/
  │   ├── app/
  │   │   ├── components/         # React компоненты
  │   │   │   ├── auth/          # Компоненты аутентификации
  │   │   │   ├── ui/            # UI компоненты (shadcn/ui)
  │   │   │   └── ...            # Страницы и функциональные компоненты
  │   │   ├── data/              # Моковые данные (для разработки)
  │   │   └── App.tsx            # Главный компонент приложения
  │   ├── contexts/              # React контексты
  │   │   └── AuthContext.tsx    # Контекст аутентификации
  │   ├── lib/                   # Библиотеки и утилиты
  │   │   ├── supabase.ts        # Клиент Supabase
  │   │   └── database.types.ts  # TypeScript типы для БД
  │   ├── services/              # Сервисы для работы с API
  │   │   ├── disciplineService.ts
  │   │   ├── groupService.ts
  │   │   └── studentService.ts
  │   ├── styles/                # Стили
  │   └── main.tsx               # Точка входа
  ├── SUPABASE_SETUP.md          # Инструкция по настройке БД
  └── README.md                  # Этот файл
  ```

  ## Архитектура

  ### Аутентификация и авторизация

  - Система использует Supabase Auth для управления пользователями
  - Три роли: **студент**, **преподаватель**, **администратор**
  - Студенты регистрируются самостоятельно, выбирая группу
  - Преподаватели регистрируются с секретным ключом
  - Администраторы создаются вручную через SQL

  ### Система доступов

  - Преподаватель создает дисциплину
  - Доступ к дисциплине выдается:
    - Целой группе студентов
    - Отдельным студентам (индивидуально)
  - Студент видит только те дисциплины, к которым у него есть доступ

  ### Безопасность

  - Row Level Security (RLS) на уровне базы данных
  - Защита маршрутов на уровне приложения
  - Проверка прав доступа для всех операций

  ## Основные компоненты

  ### AuthContext
  Управляет состоянием аутентификации пользователя, предоставляет методы для входа, регистрации и выхода.

  ### ProtectedRoute
  Компонент-обертка для защиты маршрутов, требующих аутентификации.

  ### DisciplinesPage
  Страница со списком дисциплин. Для преподавателей добавляется функционал создания и управления доступом.

  ### CreateDisciplineDialog
  Диалог для создания новой дисциплины (только для преподавателей).

  ### ManageAccessDialog
  Диалог для управления доступом к дисциплине - выдача доступа группам и отдельным студентам.

  ## Разработка

  ### Добавление новых компонентов UI

  Проект использует shadcn/ui. Для добавления новых компонентов используйте CLI:

  ```bash
  npx shadcn-ui@latest add <component-name>
  ```

  ### Работа с базой данных

  Все запросы к БД выполняются через сервисы в папке `src/services/`. Это обеспечивает:
  - Централизованную обработку ошибок
  - Типизацию данных
  - Переиспользование кода

  ### Типы данных

  TypeScript типы для базы данных находятся в `src/lib/database.types.ts`. При изменении структуры БД обновите этот файл.

  ## Развертывание

  ### Production build

  ```bash
  npm run build
  ```

  Собранные файлы будут в папке `dist/`.

  ### Рекомендации для продакшн

  1. Измените секретный ключ преподавателя в `system_settings`
  2. Настройте CORS в Supabase
  3. Включите rate limiting для API
  4. Настройте регулярные бэкапы базы данных
  5. Используйте HTTPS для всех соединений

  ## Известные ограничения MVP

  Текущая версия является MVP и включает базовый функционал:

  ✅ **Реализовано:**
  - Аутентификация и регистрация
  - Управление дисциплинами
  - Система доступов (группы и индивидуальные)
  - Базовая структура для лекций и тестов

  🚧 **Требует доработки:**
  - Редактор лекций (статьи)
  - Загрузка и конвертация PPTX презентаций
  - Система тестирования с проверкой ответов
  - Отслеживание прогресса студентов
  - Панель администратора для управления группами
  - Уведомления и email рассылки

  ## Поддержка

  При возникновении проблем:
  1. Проверьте логи в консоли браузера (F12)
  2. Проверьте логи Supabase (раздел Logs)
  3. Убедитесь, что все переменные окружения настроены
  4. Проверьте, что RLS политики применены корректно

  ## Лицензия

  Проект основан на дизайне: https://www.figma.com/design/ZhY9wLvnijyDqunx8YafjF/Educational-Platform-Design
  